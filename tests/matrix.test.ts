describe("Matix test suite", () => {

    it("should retrieve an entry of a matrix", () => {

    })

    it("should retrieve the size of a matrix as a tuple [m, n]", () => {

    })

    it("should transpose a matrix", () => {

    })

    it("should add two matrices of the same size", () => {

    })

    it("should substract two matrices of the same size", () => {

    })

    it("should multiply two matrices of the same size", () => {

    })

    it("should calculate a matrix determinant", () => {

    })

    it("should get the identity matrix", () => {

    })
})